<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/SingInFragment">

    <fragment
        android:id="@+id/SingInFragment"
        android:name="ir.blockify.ui.SingUp.SingInFragment"
        android:label="@string/first_fragment_label"
        tools:layout="@layout/fragment_firstt">

        <action
            android:id="@+id/action_SingInFragment_to_SingUpFragment"
            app:destination="@id/SingUpFragment" />
    </fragment>
    <fragment
        android:id="@+id/SingUpFragment"
        android:name="ir.blockify.ui.SingUp.SingUpFragment"
        android:label="@string/second_fragment_label"
        tools:layout="@layout/fragment_second">

        <action
            android:id="@+id/action_SingUpFragment_to_SingInFragment"
            app:destination="@id/SingInFragment" />
    </fragment>
</navigation>